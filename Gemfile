source "https://rubygems.org"

# GitHub Pages gem ensures compatibility with GitHub Pages environment
gem "github-pages", group: :jekyll_plugins

# Theme gem (replace 'minima' with your chosen theme)
gem "minima", "~> 2.5"
